<?xml version="1.0" encoding="UTF-8"?>

<xwikidoc>
  <web>EESCCode</web>
  <name>LiveTableResults</name>
  <language/>
  <defaultLanguage/>
  <translation>0</translation>
  <parent>XWiki.WebHome</parent>
  <creator>xwiki:XWiki.Admin</creator>
  <author>xwiki:XWiki.Admin</author>
  <customClass/>
  <contentAuthor>xwiki:XWiki.Admin</contentAuthor>
  <creationDate>1392737220000</creationDate>
  <date>1392737220000</date>
  <contentUpdateDate>1392737220000</contentUpdateDate>
  <version>1.1</version>
  <title>$services.localization.render('platform.livetable.results')</title>
  <defaultTemplate/>
  <validationScript/>
  <comment/>
  <minorEdit>false</minorEdit>
  <syntaxId>xwiki/2.0</syntaxId>
  <hidden>true</hidden>
  <content>{{include document="XWiki.LiveTableResultsMacros" /}}

{{velocity wiki="false"}}
#set ($extra = '')
#set ($params = [])
#if ("$!request.space" != '')
  #set ($extra = "${extra} and doc.space = ?")
  #set ($discard = $params.add($request.space))
#end
#if ("$!request.parent" != '')
  #set ($extra = "${extra} and doc.parent = ?")
  #set ($discard = $params.add($request.parent))
#end
#if ("$!request.orphaned" == '1')
  #set ($extra = "${extra} and doc.parent='' and doc.name&lt;&gt;'WebHome'")
#end

## Init variables
#set($userId = "$xcontext.getUser()")
#set($groupId = $services.wiki.getCurrentWikiId().substring(1))

## Get the list of documents
#set($xwql_query = 'where ')
#set($sep = '')
#set($groupList = $services.eesc.getUsersForGroup($groupId))
#if(!$groupList.isEmpty())
  #foreach($u in $groupList)
    #set($userId = "xwiki:XWiki.$u.getId()")
    #set($xwql_query = "${xwql_query}${sep}doc.creator = '$userId'")
    #set($sep = ' or ')
  #end
  #set($queryResult = $services.query.xwql($xwql_query).addFilter("hidden").execute())

  ## Keep only the documents that the user can access
  #if(!$queryResult.isEmpty())
    ## Open parenthesis
    #set($begin = ' and ( ')
    #set($sep = '')
    #set($end = " and doc.fullName = 'NULL'") ## Must display no document if no document are shared
    #foreach($d in $queryResult)
      #set($d = $xwiki.getDocument("$d"))
      #if($d.hasAccessLevel('view'))
        #set($extra = "${begin}${extra}${sep}doc.fullName = '${d.toString()}'")
        #set($begin = '')
        #set($sep = ' or ')
        #set($end = ' )')
      #end
    #end
    ## Close parenthesis
    #set($extra = "${extra}${end}")
  #end
#end

#gridresultwithfilter("$!request.classname" $request.collist.split(',') '' "${extra}" $params)
{{/velocity}}</content>
</xwikidoc>
